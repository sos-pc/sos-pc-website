<section id="diagnostic" class="diag-section">
  <div class="section-label">
    <span class="tag tag-teal">03</span>
    <span class="mono text-muted">// Diagnostic IA</span>
  </div>
  <div class="diag-grid">
    <div class="diag-content">
      <h2>Diagnostic PC<br /><span class="teal">Intelligent</span></h2>
      <p>Notre IA analyse en profondeur votre système Windows — CPU, RAM, disques, erreurs système — et vous propose un rapport clair avec des recommandations concrètes.</p>
      <div class="diag-features">
        <div class="df-item"><span class="teal mono">✓</span> Analyse complète en 30 secondes</div>
        <div class="df-item"><span class="teal mono">✓</span> Rapport détaillé avec priorités</div>
        <div class="df-item"><span class="teal mono">✓</span> Chat IA pour aller plus loin</div>
        <div class="df-item"><span class="teal mono">✓</span> 100% gratuit, aucune installation</div>
      </div>
      <div class="diag-privacy">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path></svg>
        Aucun fichier personnel collecté — uniquement les données techniques système.
      </div>
      <details class="diag-disclosure">
        <summary>
          <span class="dd-arrow mono">▸</span>
          <span class="dd-label mono">Que collecte exactement le script ?</span>
        </summary>
        <div class="dd-body">
          <div class="dd-col dd-col--ok">
            <p class="dd-h mono">COLLECTÉ</p>
            <ul>
              <li><span class="mono ok">✓</span> Modèle CPU, RAM, GPU</li>
              <li><span class="mono ok">✓</span> État disques (SMART, espace libre)</li>
              <li><span class="mono ok">✓</span> Version Windows et mises à jour</li>
              <li><span class="mono ok">✓</span> Erreurs système (7 derniers jours)</li>
              <li><span class="mono ok">✓</span> Logiciels installés (nom, version)</li>
              <li><span class="mono ok">✓</span> État antivirus / pare-feu</li>
              <li><span class="mono ok">✓</span> Températures matérielles</li>
            </ul>
          </div>
          <div class="dd-col dd-col--no">
            <p class="dd-h mono">NON COLLECTÉ</p>
            <ul>
              <li><span class="mono err">✗</span> Fichiers personnels</li>
              <li><span class="mono err">✗</span> Mots de passe</li>
              <li><span class="mono err">✗</span> Historique de navigation</li>
              <li><span class="mono err">✗</span> Cookies et sessions</li>
              <li><span class="mono err">✗</span> Captures d'écran</li>
              <li><span class="mono err">✗</span> Frappes clavier</li>
              <li><span class="mono err">✗</span> Données réseau (URL, contenu)</li>
            </ul>
          </div>
        </div>
        <p class="dd-foot mono">
          Le script s'exécute une seule fois, ne s'installe pas et ne laisse aucune trace.
          Le code source sera publié prochainement.
        </p>
      </details>
    </div>
    <div class="diag-terminal">
      <div class="terminal-bar">
        <span class="t-dot red"></span>
        <span class="t-dot yellow"></span>
        <span class="t-dot green"></span>
        <span class="t-title">diagnostic-talos-int.ps1</span>
      </div>
      <div class="terminal-body">
        <div class="t-line"><span class="t-prompt">PS&gt;</span> <span class="t-cmd">$s='XXXX'; irm talos-int.com/diag.ps1 | iex</span></div>
        <div class="t-output">Collecte des informations système...</div>
        <div class="t-output t-list">
          <span class="t-item ok">✓ OS Windows 11 Pro</span>
          <span class="t-item ok">✓ CPU Intel i7 — 23% load</span>
          <span class="t-item ok">✓ RAM 16 Go — 68% utilisée</span>
          <span class="t-item warn">⚠ Disque C: 94% utilisé</span>
          <span class="t-item err">✗ 3 erreurs critiques détectées</span>
        </div>
        <div class="t-output teal">Envoi des données vers Talos Int…</div>
        <div class="t-output" style="color:#00d4aa;font-weight:600">✓ Retournez sur talos-int.com !</div>
      </div>
      <div class="diag-cta-box">
        <p class="mono" style="font-size:12px;color:var(--text-muted);margin-bottom:8px;">
          // Ouvrez le chatbot → copiez la commande → collez dans PowerShell
        </p>
        <button class="btn-primary" onclick="sospcToggle()" style="width:100%;justify-content:center;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
          Lancer le diagnostic
        </button>
      </div>
    </div>
  </div>
</section>

<style>
  .teal { color: var(--accent-hex); }
  .text-muted { color: var(--text-muted); }
  .section-label {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 1rem;
  }

  .diag-section { padding: 5rem 0; }

  .diag-grid {
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: 4rem;
    align-items: start;
    margin-top: 2rem;
  }

  .diag-content h2 {
    font-size: clamp(1.8rem, 3.5vw, 2.4rem);
    margin-bottom: 1rem;
  }

  .diag-content p {
    color: var(--text-muted);
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .diag-features {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 1.5rem;
  }

  .df-item {
    font-size: 14px;
    color: var(--text);
    display: flex;
    gap: 10px;
  }

  .diag-privacy {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: var(--text-dim);
    font-family: var(--font-mono);
    background: #00d4aa0d;
    border: 1px solid rgba(0, 212, 170, 0.15);
    border-radius: var(--radius);
    padding: 8px 12px;
  }

  .diag-disclosure {
    margin-top: 12px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
  }

  .diag-disclosure[open] {
    border-color: rgba(0, 212, 170, 0.35);
  }

  .diag-disclosure summary {
    list-style: none;
    cursor: pointer;
    padding: 10px 14px;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: var(--text-muted);
    transition: background 0.2s, color 0.2s;
    user-select: none;
  }

  .diag-disclosure summary::-webkit-details-marker {
    display: none;
  }

  .diag-disclosure summary:hover {
    background: var(--bg-elevated);
    color: var(--text);
  }

  .dd-arrow {
    color: var(--accent-hex);
    transition: transform 0.2s ease;
    display: inline-block;
  }

  .diag-disclosure[open] .dd-arrow {
    transform: rotate(90deg);
  }

  .dd-label {
    font-size: 12px;
  }

  .dd-body {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    padding: 8px 14px 4px;
    border-top: 1px solid var(--border);
  }

  .dd-col {
    padding: 8px 6px;
  }

  .dd-col + .dd-col {
    border-left: 1px dashed var(--border);
    padding-left: 14px;
  }

  .dd-h {
    font-size: 10px;
    letter-spacing: 0.08em;
    margin-bottom: 8px;
    padding-bottom: 4px;
    border-bottom: 1px dashed var(--border);
  }

  .dd-col--ok .dd-h { color: var(--accent-hex); }
  .dd-col--no .dd-h { color: var(--red); }

  .dd-col ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .dd-col li {
    font-size: 12px;
    color: var(--text-muted);
    display: flex;
    gap: 8px;
    line-height: 1.4;
  }

  .dd-col .ok { color: var(--accent-hex); flex-shrink: 0; }
  .dd-col .err { color: var(--red); flex-shrink: 0; }

  .dd-foot {
    font-size: 11px;
    color: var(--text-dim);
    padding: 8px 14px 12px;
    line-height: 1.5;
  }

  @media (max-width: 480px) {
    .dd-body {
      grid-template-columns: 1fr;
    }
    .dd-col + .dd-col {
      border-left: none;
      border-top: 1px dashed var(--border);
      padding-left: 6px;
      padding-top: 12px;
      margin-top: 4px;
    }
  }

  .diag-terminal {
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    overflow: hidden;
    box-shadow: 0 0 30px #0006;
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    background: var(--bg-elevated);
    padding: 10px 16px;
    border-bottom: 1px solid var(--border);
  }

  .t-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }
  .t-dot.red { background: #f7768e; }
  .t-dot.yellow { background: #e0af68; }
  .t-dot.green { background: #00d4aa; }

  .t-title {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-muted);
    margin-left: 8px;
  }

  .terminal-body {
    background: #0d0e14;
    padding: 20px;
    font-family: var(--font-mono);
    font-size: 13px;
    line-height: 1.8;
  }

  .t-line {
    display: flex;
    gap: 8px;
    align-items: center;
  }

  .t-prompt {
    color: var(--accent-hex);
    font-weight: 600;
  }

  .t-cmd { color: var(--text); }

  .t-output {
    color: var(--text-muted);
    padding-left: 16px;
  }

  .t-output.teal { color: var(--accent-hex); }

  .t-list {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .t-item { display: block; }
  .t-item.ok { color: #00d4aa; }
  .t-item.warn { color: #e0af68; }
  .t-item.err { color: #f7768e; }

  .diag-cta-box {
    padding: 16px;
    background: var(--bg-elevated);
    border-top: 1px solid var(--border);
  }

  @media (max-width: 900px) {
    .diag-grid { grid-template-columns: 1fr; gap: 2rem; }
  }
</style>
